from setuptools import setup, find_packages

setup(
    name="pysettings",
    version="0.1",
    description="A package that saves and loads",
    url="https://github.com/sryu1/pysettings",
    author="sryu1",
    license="",
    install_requires=[],
    packages=find_packages(),
    classifiers=[],
    python_requires=">=3.7",
    include_package_data=True,
    zip_safe=False,
)